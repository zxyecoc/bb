@model News
@using Microsoft.AspNetCore.Mvc.Localization
@using Microsoft.Extensions.Localization
@using LAB1.Resources
@inject IHtmlLocalizer<SharedResources> Localizer

<div class="manga-details-container">
    <!-- Ліва панель: обкладинка та основна інформація -->
    <div class="manga-left-panel">
        <div class="manga-cover-container">
            <img src="@Model.CoverUrl" alt="@Model.Title" class="manga-cover" />
        </div>
        <div class="manga-info">
            <h1>@Model.Title</h1>
            <p><strong>@Localizer["Author"]:</strong> @Model.Author.Name</p>
        </div>
    </div>

    <!-- Права панель: розділи, рейтинг та коментарі -->
    <div class="manga-right-panel">
        @if (User.Identity.IsAuthenticated)
        {
            <div class="rating-button-container">
                <form asp-action="AddRating" method="post">
                    <input type="hidden" name="newsId" value="@Model.Id" />
                    <label for="rating">@Localizer["AddRating"]:</label>
                    <select name="ratingValue" id="rating">
                        <option value="1">1</option>
                        <option value="2">2</option>
                        <option value="3">3</option>
                        <option value="4">4</option>
                        <option value="5">5</option>
                        <option value="6">6</option>
                        <option value="7">7</option>
                        <option value="8">8</option>
                        <option value="9">9</option>
                        <option value="10">10</option>
                    </select>
                    <button type="submit">@Localizer["Apply"]</button>
                </form>
            </div>
        }
        else
        {
            <p>@Localizer["OnlyAuthorizedUsersCanAddRatings"]</p>
        }
        <div class="manga-description">
            <h2>@Localizer["Description"]</h2>
            <p>@Model.NewsText</p>
        </div> 

        <div class="tags-container">
            <strong>@Localizer["Tags"]:</strong>
            @foreach (var tag in Model.Tags)
            {
                <span class="tag">@tag.Name</span>
            }
        </div>

        <!-- Блок коментарів -->
        @*<div class="comments-section">
            <h3>@Localizer["Comments"]:</h3>

            <!-- Відображення повідомлень про помилки чи успіх -->
            @if (TempData["Error"] != null)
            {
                <div class="alert alert-danger">@TempData["Error"]</div>
            }

            @if (TempData["Success"] != null)
            {
                <div class="alert alert-success">@TempData["Success"]</div>
            }
            <div class="add-comment-container">
                @if (User.Identity.IsAuthenticated)
                {
                    <form asp-action="AddComment" method="post">
                        <input type="hidden" name="newsId" value="@Model.Id" />
                        <textarea name="content" required placeholder="@Localizer["WriteYourComment"]" class="form-control"></textarea>
                        <button type="submit" class="btn btn-success">@Localizer["AddComment"]</button>
                    </form>
                }
                else
                {
                    <p>@Localizer["OnlyAuthorizedUsersCanWriteComments"]</p>
                }
            </div>
            <div class="comments-list">
                @foreach (var comment in Model.Comments.OrderByDescending(c => c.CreatedAt))
                {
                    <div class="comment">
                        <p><strong>@comment.UserName</strong> @comment.CreatedAt.ToString("dd.MM.yyyy HH:mm")</p>
                        <p>@comment.Content</p>
                        @if (User.IsInRole("Administrator"))
                        {
                            <!-- Кнопка для видалення коментаря -->
                            <form asp-action="DeleteComment" method="post" style="display:inline;" onsubmit="return confirm('Ви впевнені, що хочете видалити цей коментар?');">
                                <form asp-action="DeleteComment" method="post" style="display:inline;">
                                    <input type="hidden" name="commentId" value="@comment.Id" />
                                    <input type="hidden" name="newsId" value="@Model.Id" />
                                    <button type="submit" class="btn btn-danger btn-sm">Видалити</button>
                                </form>
                            </form>
                        }
                    </div>
                }
            </div>*@
        </div>
        <!-- JavaScript для перевірки коментаря -->
        <script>
            document.getElementById("commentForm").addEventListener("submit", function (event) {
                const content = document.getElementById("commentContent").value;

                // Перевірка довжини коментаря
                if (content.length < 5 || content.length > 500) {
                    alert(@Localizer["CommentLenghtAllert"]);
                    event.preventDefault(); // Зупиняємо відправку форми
                }
            });
        </script>
    </div>
</div>


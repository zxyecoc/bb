@model IEnumerable<LAB1.Models.Manga>
@using Microsoft.AspNetCore.Mvc.Localization
@using Microsoft.Extensions.Localization
@using LAB1.Resources
@inject IHtmlLocalizer<SharedResources> Localizer

@if (ViewBag.SearchQuery == null)
{
    <h1>@Localizer["MangaCatalog"]</h1>
}
else 
{
    <h1>@Localizer["Results"]: "@ViewBag.SearchQuery"</h1>
}


<div class="catalog-container">

    @if (ViewBag.SearchQuery != null)
    {
        @if (Model != null && Model.Any())
        {
            <div class="manga-container">
                @foreach (var manga in Model)
                {
                    <div class="manga-item">
                        <a href="@Url.Action("MangaDetails", "Mangas", new { id = manga.Id })" class="manga-link">
                            <img src="@manga.CoverUrl" alt="@manga.Title" class="manga-cover" />
                            <h3>@manga.Title</h3>
                        </a>
                    </div>

                }
            </div>
        }
        else
        {
            <p>@Localizer["MangaResults"] "@ViewBag.SearchQuery" @Localizer["NotFound"].</p>
        }
    }
    else
    {
        <div class="manga-container">
            @foreach (var manga in Model)
            {
                <div class="manga-item">
                    <a href="@Url.Action("MangaDetails", "Mangas", new { id = manga.Id })" class="manga-link">
                        <img src="@manga.CoverUrl" alt="@manga.Title" class="manga-cover" />
                        <h3>@manga.Title</h3>
                    </a>
                </div>
            }
        </div>
    }

    <!-- Панель фільтрації -->
    <div class="filter-panel">
        <h3>@Localizer["Filtration"]</h3>
        <form asp-action="Index" method="get">
            <!-- Фільтр за тегами -->
            <div class="form-group">
                <label>@Localizer["Tags"]:</label><br />
                @if (ViewBag.Tags != null)
                {
                    @foreach (var tag in ViewBag.Tags as List<LAB1.Models.Tag>)
                    {
                        <div class="form-check">
                            <input type="checkbox" name="selectedTags" value="@tag.Id" class="form-check-input" />
                            <label class="form-check-label">@tag.Name</label>
                        </div>
                    }
                }
                else
                {
                    <p>@Localizer["NoTags"]</p>
                }
            </div>

            <!-- Фільтр за автором -->
            <div class="form-group">
                <label>@Localizer["Author"]:</label>
                <select name="authorId" class="form-control">
                    <option value="">@Localizer["AllAuthor"]</option>
                    @foreach (var author in ViewBag.Authors as List<LAB1.Models.Author>)
                    {
                        <option value="@author.Id">@author.Name</option>
                    }
                </select>
            </div>

            <!-- Фільтр за ілюстратором -->
            <div class="form-group">
                <label>@Localizer["Illustrator"]:</label>
                <select name="illustratorId" class="form-control">
                    <option value="">@Localizer["AllIllustrator"]</option>
                    @foreach (var illustrator in ViewBag.Authors as List<LAB1.Models.Author>)
                    {
                        <option value="@illustrator.Id">@illustrator.Name</option>
                    }
                </select>
            </div>

            <!-- Сортування -->
            <h3>@Localizer["Sorting"]</h3>
            <div class="form-group">
                <label>@Localizer["SortBy"]:</label>
                <select name="sortOrder" class="form-control">
                    <option value="title">@Localizer["Title"]</option>
                    <option value="year">@Localizer["ReleaseYear"]</option>
                    <option value="rating">@Localizer["Rating"]</option>
                    <option value="chapters">@Localizer["ChapterNumber"]</option>
                </select>
            </div>

            <!-- Кнопки -->
            <div class="form-group">
                <button type="submit" class="btn btn-primary">@Localizer["Apply"]</button>
                <a href="@Url.Action("Index", "Catalog")" class="btn btn-secondary">@Localizer["Reset"]</a>
            </div>
        </form>
    </div>


</div>



